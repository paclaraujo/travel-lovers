import { loginTemplate } from '../pages/login/loginView.js';
import { registerTemplate } from '../pages/register/registerView.js';
import { feedTemplate } from '../pages/feed/feedView.js';

export const routes = {
  login: loginTemplate,
  register: registerTemplate,
  feed: feedTemplate
}